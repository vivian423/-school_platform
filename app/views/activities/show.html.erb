<h1>Activity Show Page </h1>

<div>
  <canvas id="myChart"></canvas>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
  const ctx = document.getElementById('myChart');

  new Chart(ctx, {
    type: 'bar',
    data: {
      labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
      datasets: [{
        label: '# of Votes',
        data: [12, 19, 3, 5, 2, 3],
        borderWidth: 1
      }]
    },
    options: {
      scales: {
        y: {
          beginAtZero: true
        }
      }
    }
  });
</script>
 









<div>



<%# Commented out the below for pundit reference later - needs work%>
  <%# <% if policy(@activity).edit? %>
    <%# <%= link_to "Edit activity",edit_activity_path(@activity)  %> %>
  <%# <% end %>

  <%# <%= link_to "Delete this activity", @activity, method: :delete if policy(@activity).destroy? %> %>

  <%# <%= link_to "Create new activity", new_activity_path if policy(Activity).new? %> %>

</div>
